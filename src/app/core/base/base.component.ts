import { ActivePage } from "../../enums/active-page.enum";
import { TypeButton } from "../../enums/button-type.enum";

export class BaseComponent {
    public activePageEnum = ActivePage;
    public typeButtonEnum = TypeButton;
}